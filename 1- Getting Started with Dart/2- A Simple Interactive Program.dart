// A Simple Interactive Program

import 'dart:io';
// we are importing the dart:io library. In computer programming, 
//a library is a collection of similar code that you can reference in your own code. 
// When you import a library, you can access all the code in that library.
void main() {
    print("Hello " + stdin.readLineSync()!);
    //we will be using the readLineSync() method (function) which reads a line from the input.
    // stdin stands for standard input,
}

